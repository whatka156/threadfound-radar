v17 full-screen edit page.

Replace these in GitHub:
- index.html
- service-worker.js

What changed:
- Swipe Edit now opens a full-screen edit page instead of the fragile edit popup.
- Keyboard should behave much better because it is a normal scrollable page.
- Bottom nav and app header hide while editing.
- Back/Cancel returns to the tab you came from.
- Save updates the item and keeps existing sold/loss details.
- Already owned is supported in the edit page.
